{
  "scripts": {
    "test": "jest",
    "test:smoke": "jest --selectProjects smoke",
    "test:unit": "jest --selectProjects unit",
    "test:integration": "jest --selectProjects integration",
    "test:contract": "jest --selectProjects contract",
    "test:performance": "jest --selectProjects performance",
    "test:e2e": "jest --selectProjects e2e",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:quick": "jest --selectProjects smoke unit",
    "test:fast": "jest --selectProjects smoke unit integration --testTimeout=30000",
    "test:ci": "jest --ci --coverage --watchAll=false",
    "test:debug": "jest --detectOpenHandles --verbose"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "@types/jest": "^29.0.0",
    "supertest": "^6.0.0"
  }
}